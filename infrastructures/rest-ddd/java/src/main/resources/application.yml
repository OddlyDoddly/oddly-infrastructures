spring:
  application:
    name: rest-ddd-java-template

  # Database configuration (example - MongoDB)
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: example_db

  # RabbitMQ configuration (example)
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Server configuration
server:
  port: 8080

# Logging
logging:
  level:
    root: INFO
    com.example.restddd: DEBUG
